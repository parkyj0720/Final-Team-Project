<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="signXml">

<select id="idCheck" resultType="int" parameterType="String">
	select count(*)
	from member
	where mem_id = #{userId}
</select>


<select id="signCheck" resultType="String" parameterType="Member.MemberDto">
	select mem_pw
	from member
	where mem_id=#{userId}
</select>

<select id="sessionInput" parameterType="SessionDto">
	
</select>






</mapper>